import {createRoutine} from "redux-saga-routines";

const GET_REGIONS = createRoutine("GET_REGIONS");
const GET_REGION= createRoutine("GET_REGION");
const CREATE_REGION = createRoutine("CREATE_REGION");
const UPDATE_REGION = createRoutine("UPDATE_REGION");
const DELETE_REGION = createRoutine("DELETE_REGION");
const TOGGLE_BURGER = createRoutine("TOGGLE_BURGER");
const STATISTICS_PLACE_ACTION_COUNTS = createRoutine("STATISTICS_PLACE_ACTION_COUNTS");
const STATISTICS_ORDER_COUNTS = createRoutine("STATISTICS_ORDER_COUNTS");
const STATISTICS_TYPE_VIOLENCE = createRoutine("STATISTICS_TYPE_VIOLENCE");
const STATISTICS_VICTIM_COUNT = createRoutine("STATISTICS_VICTIM_COUNT");
const GET_MONTHLY_STATISTICS = createRoutine("GET_MONTHLY_STATISTICS");

export default {
    GET_REGIONS,
    GET_REGION,
    CREATE_REGION,
    UPDATE_REGION,
    DELETE_REGION,
    TOGGLE_BURGER,
    STATISTICS_PLACE_ACTION_COUNTS,
    STATISTICS_ORDER_COUNTS,
    STATISTICS_TYPE_VIOLENCE,
    STATISTICS_VICTIM_COUNT,
    GET_MONTHLY_STATISTICS
}
